# Templates CMakeLists.txt

# Only build minimal_game as template
add_subdirectory(minimal_game)

# Note: simple_game and full_game are kept as reference examples
# but not built by default. Users can copy them to their own projects.
